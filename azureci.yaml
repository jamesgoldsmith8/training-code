name: $(date:yyyymmdd)$(rev:.rr)

triggers:
    - master

stages:
    - stage: compile
        jobs:
            - job: aspnet
                pool:
                    vmImage: ubuntu-1804
                steps:
                    - script: |
                        dotnet build 3_devops/Devops.sln
    # - stage: test
    # - stage: pack
    # - stage: deploy

